<process xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../petriflow_schema.xsd">
	<id>impersonation_users_select</id>
	<version>1.0.0</version>
	<title id="title">Impersonators configuration - user select</title>
	<icon>home</icon>
	<defaultRole>false</defaultRole>
	<anonymousRole>false</anonymousRole>
	<properties>
		<property key="initials">IPU</property>
	</properties>
	<processEvents/>
	<caseEvents/>
	<data type="text">
		<id>parentId</id>
		<title/>
	</data>
	<data type="enumeration_map">
		<id>select_impersonator</id>
		<title id="select_impersonator">Select impersonator</title>
		<placeholder id="select_impersonator_placeholder">Search by name or email address</placeholder>
		<component>
			<id>autocomplete_dynamic</id>
		</component>
		<event type="set">
			<id>select_impersonator_set</id>
			<actions phase="post">
				<action id="action_25">
					<![CDATA[
					if (!select_impersonator.value) {
						change select_impersonator options { [:] }
					} else if (!select_impersonator.options.containsKey(select_impersonator.value)) {
						def selectableUsers = userService.searchAllCoMembers(
								select_impersonator.value,
								userService.identity.transformToLoggedUser(),
								false,
								new com.netgrif.application.engine.utils.FullPageRequest()).content
						change select_impersonator options {
							[*selectableUsers].sort { it?.surname }.collectEntries { [(it.id): ("${it?.fullName} ($it.email)" as String)] }
						}
					}
					]]>
				</action>
			</actions>
		</event>
	</data>
	<i18n locale="sk">
		<i18nString id="select_impersonator">Výber zastupiteľa</i18nString>
		<i18nString id="select_impersonator_placeholder">Vyhľadajte podľa mena alebo emailovej adresy</i18nString>
		<i18nString id="title">Konfigurácia zastupiteľov - user select</i18nString>
	</i18n>
	<i18n locale="de">
		<i18nString id="select_impersonator">Vertreter auswählen</i18nString>
		<i18nString id="select_impersonator_placeholder">Suche nach Name oder E-Mail-Adresse</i18nString>
		<i18nString id="title">Vertreter Konfiguration - user select</i18nString>
	</i18n>
	<transition>
		<id>select_user</id>
		<x>500</x>
		<y>180</y>
		<title>[select user]</title>
	</transition>
</process>